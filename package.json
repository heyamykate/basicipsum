{
	"name": "server",
	"version": "1.0.0",
	"description": "",
	"engines": {
		"node": "10.x"
	},
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node index.js",
		"heroku-postbuild": "cd frontend && npm install --only=dev && npm install && npm run build"
	},
	"author": "amy-kate andrews (amykate.andrews@gmail.com)",
	"license": "ISC",
	"dependencies": {
		"@emotion/core": "^10.0.10",
		"body-parser": "^1.19.0",
		"express": "^4.16.4"
	}
}
